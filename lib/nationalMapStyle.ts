// The fill stroke masks concatenated municipality seams. The visible outer
// boundary is half the difference between the silhouette and fill widths.
export const nationalMapStroke = {
  color: "#263744",
  opacity: 0.78,
  silhouetteWidth: 4.9,
  fillWidth: 2.7
} as const;
